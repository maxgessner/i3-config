# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

#Needed for i3-gaps
#hide_edge_borders both
#for_window [class="^.*"] border normal 1
for_window [class="^.*"] border pixel 2
#default_border normal 1
default_border pixel 2
gaps inner 2
gaps outer 0

#bindsym $mod+z		gaps outer current plus 5
#bindsym $mod+Shift+z    gaps outer current minus 5
#bindsym $mod+u          gaps inner current plus 5
#bindsym $mod+Shift+u    gaps inner current minus 5

mode "resize_gaps_outer" {
        bindsym plus    gaps outer current plus 5
        bindsym minus   gaps outer current minus 5
        bindsym 0       gaps outer current set 0

        bindsym Shift+plus  gaps outer all plus 5
        bindsym Shift+minus gaps outer all minus 5
        bindsym Shift+0     gaps outer all set 0

        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+Shift+o mode "default"
        bindsym $mod+o mode "default"
}
bindsym $mod+Shift+o mode "resize_gaps_outer"

mode "resize_gaps_inner" {
        bindsym plus    gaps inner current plus 5
        bindsym minus   gaps inner current minus 5
        bindsym 0       gaps inner current set 0

        bindsym Shift+plus  gaps inner all plus 5
        bindsym Shift+minus gaps inner all minus 5
        bindsym Shift+0     gaps inner all set 0

        bindsym Return mode "default-"
        bindsym Escape mode "default"
        bindsym $mod+Shift+i mode "default"
        bindsym $mod+i mode "default"
}
bindsym $mod+Shift+i mode "resize_gaps_inner"

# Only enable gaps on a workspace when there is at least one container
smart_gaps on

# set default orientation of new workspaces
default_orientation auto

# set default orientation of new containers
workspace_layout default

#set $term --no-startup-id i3-sensible-terminal
set $logout i3-msg exit

# load arandr default configuration
exec --no-startup-id  "sh -c 'sleep 1; exec ~/.screenlayout/default.sh'"
# #---Starting External Scripts---# #
# Setting the background:
exec --no-startup-id "sh -c 'sleep 1; exec nitrogen --restore'"
exec_always --no-startup-id nitrogen --restore 
# Starts dunst for notifications:
# exec --no-startup-id dunst 
#-conf ~/.config/dunst/dunstrc
# Composite manager:
#exec_always --no-startup-id "sh -c 'compton -cCb -i 0.8 --detect-rounded-corners --focus-exclude 'name="i3lock"''"
exec_always --no-startup-id "sh -c 'compton -cCb -i 0.8 --detect-rounded-corners --config ~/.config/i3/compton.conf'"
# exec_always --no-startup-id xcompmgr -n &
# exec_always --no-startup-id xcompmgr -c &
#exec_always --no-startup-id xcompmgr -s &
#exec always --no-startup-id xcompmgr -a &

# get prompts for root password again
exec_always --no-startup-id /usr/lib/policykit-1-gnome/polkit-gnome-authentication-agent-1 &

# start xfce settings service
exec --no-startup-id xfsettingsd --sm-client-disable &

# start xfce volume service
exec --no-startup-id xfce4-volumed &

# start indicator-sound-switcher to switch output via tray symbol
exec_always --no-startup-id indicator-sound-switcher &

# start xfce power manager service
exec --no-startup-id xfce4-power-manager &

# start thunar daemon
exec --no-startup-id thunar --daemon &

# start network manager
exec_always --no-startup-id nm-applet &

# handle usb drives using udiskie
# exec_always --no-startup-id udiskie &

# start orage calendar
exec_always --no-startup-id orage -t &

# start f.lux
exec --no-startup-id fluxgui &

# make pavucoltrol floating
for_window [instance="pavucontrol"] floating enable 

# make appfinder floating
for_window [instance="xfce4-appfinder"] floating enable

# Pulse Audio controls
#bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume 0 +5% , exec pkill -SIGRTMIN+10 i3blocks #increase sound volume
#bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume 0 -5% , exec pkill -SIGRTMIN+10 i3blocks #decrease sound volume
#bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute 0 toggle , exec pkill -SIGRTMIN+10 i3blocks # mute sound

# Sreen brightness controls
bindsym XF86MonBrightnessUp exec xbacklight -inc 20 # increase screen brightness
bindsym XF86MonBrightnessDown exec xbacklight -dec 20 # decrease screen brightness

set $mod Mod4
set $alt Mod1

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
#font pango:monospace 8
#font pango: hack 7 
font pango: noto sans 8 

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $alt

# disabling the annoying "ls" command in the terminal
bindsym $alt+l nope

# start a terminal
# set $TERMINAL '/usr/bin/xfce4-terminal'
bindsym $mod+Return exec i3-sensible-terminal
# bindsym $mod+Return exec xfce4-terminal 

# kill focused window
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
#bindsym $mod+d exec dmenu_run
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
 #bindsym $mod+d exec --no-startup-id i3-dmenu-desktop
 #bindsym $mod+d exec --no-startup-id rofi
# bindsym $mod+d exec "rofi -modi combi,window,drun -show combi -sidebar-mode terminal terinator -font 'hack 9' -color-window '#000000, #000000, #000000' -color-normal '#000000, #b3e774, #000000, #b3e774, #000000' -color-active '#000000, #b3e774, #000000, #b3e774, #000000' -color-urgent '#000000, #b3e774, #000000, #b3e774, #000000'"

#bindsym $mod+d exec "rofi -modi combi,window,drun -show combi -sidebar-mode terminal terinator -font 'hack 9'"
#bindsym $mod+d exec "rofi -combi-modi window,drun -show combi -modi combi,window,drun -sidebar-mode terminal xfce4-terminal -font 'hack 9'"
#bindsym Ctrl+space exec "rofi -combi-modi window,drun -show combi -modi combi,window,drun -sidebar-mode terminal xfce4-terminal -font 'hack 9'"
#bindsym Ctrl+space exec "rofi -combi-modi window,drun -show combi -modi combi,window,drun -sidebar-mode terminal xfce4-terminal -font 'hack 9' -theme Adapta-Nokto"
bindsym Ctrl+space exec "rofi -combi-modi window,drun -show drun -modi drun,combi,window -sidebar-mode -show-icons -font 'hack 9' -theme sidebar -m -4"

# bindsym $alt+Tab exec "rofi -modi combi,window,drun -show window -sidebar-mode terminal terinator -font 'hack 9' -color-window '#000000, #000000, #000000' -color-normal '#000000, #b3e774, #000000, #b3e774, #000000' -color-active '#000000, #b3e774, #000000, #b3e774, #000000' -color-urgent '#000000, #b3e774, #000000, #b3e774, #000000'"

bindsym $alt+Tab exec "rofi -modi combi,window,drun -show window -sidebar-mode -show-icons -font 'hack 9' -theme sidebar -m -4"

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+Ctrl+h split h

# split in vertical orientation
bindsym $mod+Ctrl+v split v

# toggle split orientation of container
bindsym $mod+t split toggle

# ME
# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# sticky window toggle
bindsym $mod+Shift+s sticky toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
bindsym $mod+Shift+a focus child

# dynamically rename workspaces
#exec_always --no-startup-id exec $HOME/.cargo/bin/i3wsr &
#exec_always --no-startup-id exec i3-workspace-names-daemon &
exec_always --no-startup-id exec python3 $HOME/.config/i3/i3scripts/autoname_workspaces.py --norenumber_workspaces &

# i3 alternating layout
# needs i3-py to be installed via pip3 (sudo pip3 install i3-py)
#exec_always --no-startup-id exec python3 $HOME/.config/i3/i3scripts/i3-alternating-layout/alternating_layouts.py &
exec_always --no-startup-id i3altlayout

# i3 layout-manager
# save and restore window layout using rofi and some other programs
bindsym $mod+F10 exec $HOME/.config/i3/i3scripts/i3-layout-manager/layout_manager.sh

# Define names for default workspaes for which we configure key bindings later on.
## We use variables to avoid repeating the names in multiple places.
#set $ws1 "1"
#set $ws2 "2"
#set $ws3 "3"
#set $ws4 "4"
#set $ws5 "5"
#set $ws6 "6"
#set $ws7 "7"
#set $ws8 "8"
#set $ws9 "9"
#set $ws10 "10"

#### here is new

#exec_always --no-startup-id "killall mws-server; sleep 0.5; mws-server"
#bindsym $mod+1 exec --no-startup-id "mws-client 1 k"
#bindsym $mod+2 exec --no-startup-id "mws-client 2 k"
#bindsym $mod+3 exec --no-startup-id "mws-client 3 k"
#bindsym $mod+4 exec --no-startup-id "mws-client 4 k"
#bindsym $mod+5 exec --no-startup-id "mws-client 5 k"
#bindsym $mod+6 exec --no-startup-id "mws-client 6 k"
#bindsym $mod+7 exec --no-startup-id "mws-client 7 k"
#bindsym $mod+8 exec --no-startup-id "mws-client 8 k"
#bindsym $mod+9 exec --no-startup-id "mws-client 9 k"
#bindsym $mod+0 exec --no-startup-id "mws-client 0 k"
#
#bindsym $alt+1 exec --no-startup-id "mws-client 1 s"
#bindsym $alt+2 exec --no-startup-id "mws-client 2 s"
#bindsym $alt+3 exec --no-startup-id "mws-client 3 s"
#bindsym $alt+4 exec --no-startup-id "mws-client 4 s"
#bindsym $alt+5 exec --no-startup-id "mws-client 5 s"
#bindsym $alt+6 exec --no-startup-id "mws-client 6 s"
#bindsym $alt+7 exec --no-startup-id "mws-client 7 s"
#bindsym $alt+8 exec --no-startup-id "mws-client 8 s"
#bindsym $alt+9 exec --no-startup-id "mws-client 9 s"
#bindsym $alt+0 exec --no-startup-id "mws-client 0 s"
#
#bindsym $mod+Shift+1 exec --no-startup-id "mws-client 1 m"
#bindsym $mod+Shift+2 exec --no-startup-id "mws-client 2 m"
#bindsym $mod+Shift+3 exec --no-startup-id "mws-client 3 m"
#bindsym $mod+Shift+4 exec --no-startup-id "mws-client 4 m"
#bindsym $mod+Shift+5 exec --no-startup-id "mws-client 5 m"
#bindsym $mod+Shift+6 exec --no-startup-id "mws-client 6 m"
#bindsym $mod+Shift+7 exec --no-startup-id "mws-client 7 m"
#bindsym $mod+Shift+8 exec --no-startup-id "mws-client 8 m"
#bindsym $mod+Shift+9 exec --no-startup-id "mws-client 9 m"
#bindsym $mod+Shift+0 exec --no-startup-id "mws-client 0 m"
#
#bindsym $alt+Shift+1 exec --no-startup-id "mws-client 1 w"
#bindsym $alt+Shift+2 exec --no-startup-id "mws-client 2 w"
#bindsym $alt+Shift+3 exec --no-startup-id "mws-client 3 w"
#bindsym $alt+Shift+4 exec --no-startup-id "mws-client 4 w"
#bindsym $alt+Shift+5 exec --no-startup-id "mws-client 5 w"
#bindsym $alt+Shift+6 exec --no-startup-id "mws-client 6 w"
#bindsym $alt+Shift+7 exec --no-startup-id "mws-client 7 w"
#bindsym $alt+Shift+8 exec --no-startup-id "mws-client 8 w"
#bindsym $alt+Shift+9 exec --no-startup-id "mws-client 9 w"
#bindsym $alt+Shift+0 exec --no-startup-id "mws-client 0 w"


#### here is end


# switch to workspace
bindsym $mod+1 workspace number 1
bindsym $mod+2 workspace number 2
bindsym $mod+3 workspace number 3
bindsym $mod+4 workspace number 4
bindsym $mod+5 workspace number 5
bindsym $mod+6 workspace number 6
bindsym $mod+7 workspace number 7
bindsym $mod+8 workspace number 8
bindsym $mod+9 workspace number 9
bindsym $mod+0 workspace number 10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number 1
bindsym $mod+Shift+2 move container to workspace number 2
bindsym $mod+Shift+3 move container to workspace number 3
bindsym $mod+Shift+4 move container to workspace number 4
bindsym $mod+Shift+5 move container to workspace number 5
bindsym $mod+Shift+6 move container to workspace number 6
bindsym $mod+Shift+7 move container to workspace number 7
bindsym $mod+Shift+8 move container to workspace number 8
bindsym $mod+Shift+9 move container to workspace number 9
bindsym $mod+Shift+0 move container to workspace number 10

# move focused workspace between monitors
bindsym $alt+Control+Left move workspace to output left
bindsym $alt+Control+Right move workspace to output right

# move workspace on output
bindsym $alt+Left workspace prev_on_output
#bindsym $alt+H workspace prev_on_output
bindsym $alt+Right workspace next_on_output
#bindsym $alt+L workspace next_on_output

# move window to workspace number
bindsym $mod+Shift+m exec i3-input -F 'move window to workspace "%s"' -l 2 -P 'move to workspace: '

# go to specific workspace
bindsym $mod+m exec i3-input -F 'workspace number "%s"' -l 2 -P 'go to workspace: '

# workspace back and forth
workspace_auto_back_and_forth yes

# bind workspaces to monitors
set $ws99 "99" output primary
#set workspace number 99 output primary
bindsym $alt+e workspace number 99
set $ws98 "98" output primary
#set workspace number 98 output primary

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

# disable focus follows mouse
focus_follows_mouse no

# disable mouse warping
mouse_warping none

# focus wrapping off
focus_wrapping no

# The middle button over a titlebar kills the window
bindsym --release button2 kill

# The right button toggles floating
bindsym $alt+button3 floating toggle

# adjust volume with keys
bindsym $mod+plus exec "pactl set-sink-volume 0 +2%"
bindsym $mod+minus exec "pactl set-sink-volume 0 -2%"

# border changing
bindsym $mod+b border toggle

# hide borders on screens with just one window
hide_edge_borders none 

# no borders for popup windows
for_window [window_type="popup_menu"] border none
for_window [window_type="notification"] border none

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 5 px or 5 ppt
        bindsym j resize grow height 5 px or 5 ppt
        bindsym k resize shrink height 5 px or 5 ppt
        bindsym l resize grow width 5 px or 5 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 5 px or 5 ppt
        bindsym Down resize grow height 5 px or 5 ppt
        bindsym Up resize shrink height 5 px or 5 ppt
        bindsym Right resize grow width 5 px or 5 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

# class                 border  backgr. text    indicator
client.focused #586e75 #586e75 #fdf6e3 #268bd2
client.focused_inactive #073642 #073642 #93a1a1 #002b36
client.unfocused #002b36 #002b36 #586e75 #002b36
client.urgent #dc322f #dc322f #fdf6e3 #dc322f

#exec --no-startup-id xfce4-panel --disable-wm-check &
exec_always --no-startup-id synapse -s
for_window [class="Synapse"] border none
#exec --no-startup-id spotify
exec --no-startup-id clipit &
#exec --no-startup-id dropbox start
for_window [class="Spotify"] move container to workspace number 98
assign [class="Spotify"] → number 98
#bindsym $meta+F9 [class="Spotify"] move scratchpad
for_window [class="Spotify"] move scratchpad
bindsym $mod+F9 [class="Spotify"] scratchpad show
#bindsym $mod+shift+F9 [class="Spotify"] floating toggle, move container to workspace number 98

# for_window [class="Firefox"] move container to workspace 1
for_window [class="Hexchat"] move container to workspace number 99
#exec --no-startup-id /opt/telegram/telegram
exec --no-startup-id telegram-desktop &
#exec --no-startup-id signal-desktop --start-in-tray &
#for_window [class="TelegramDesktop"] move container to workspace number 99
assign [class="TelegramDesktop"] → number 99
assign [class="Signal"] → number 99
for_window [class="Rambox"] move container to workspace number 99
for_window [class="Nitrogen"] floating enable

# make calendar widget float
for_window[title="Calendar selection"] floating enable

# Dropdown Terminal set to postion and floating mode
# bindsym $mod+t exec --no-startup-id tilda :w
#

# make xfce4-notification bottom right
for_window [class="Xfce4-notifyd"] border none

# exec --no-startup-id franz
for_window [instance="franz"] move container to workspace number 99 
# for_window [window_role="conversation"] move container to workspace "99" 
#bindsym Mod1+e [window_role="conversation"] workspace "Chat" 

for_window [class="Matplotlib"] floating enable 
for_window [class="Matplotlib"] border normal 1 
no_focus [class="Matplotlib"]

#exec --no-startup-id volumeicon

#bindsym Mod1+Control+l exec i3lock -d -c 000000 -n -f
bindsym Mod1+Control+l exec "dbus-send --print-reply --dest=org.mpris.MediaPlayer2.spotify /org/mpris/MediaPlayer2 org.mpris.MediaPlayer2.Player.Stop"; exec i3lock -d -c 000000 -n -f
#bindsym $alt_r+Control+l exec i3lock -d -c 000000 -n
bindsym XF86ScreenSaver exec i3lock -d -c 000000 -n -f
bindsym XF86Sleep exec "systemctl suspend && i3lock -d -c 000000 -n -f"


#exec --no-startup-id tilda -t 20
#for_window [class="tilda"] floating enable

exec_always --no-startup-id guake --hide &
for_window [instance="guake"] border none

show_marks yes

bindsym $mod+i exec i3-input
#bindsym $mod+m exec i3-input -F 'mark "%s"' -P 'Set mark for window: '
# set one character mark for focused window
bindsym $mod+n exec i3-input -F 'mark %s' -l 1 -P 'Mark: '
# unset one character mark
bindsym $mod+shift+n exec i3-input -F 'unmark %s' -l 1 -P 'Unmark: '
# goto marked window
bindsym $mod+g exec i3-input -F '[con_mark="%s"] focus' -l 1 -P 'Goto: '

bindsym $alt+x exec "dbus-send --print-reply --dest=org.mpris.MediaPlayer2.spotify /org/mpris/MediaPlayer2 org.mpris.MediaPlayer2.Player.PlayPause" 
bindsym $alt+v exec "dbus-send --print-reply --dest=org.mpris.MediaPlayer2.spotify /org/mpris/MediaPlayer2 org.mpris.MediaPlayer2.Player.Next" 
bindsym $alt+y exec "dbus-send --print-reply --dest=org.mpris.MediaPlayer2.spotify /org/mpris/MediaPlayer2 org.mpris.MediaPlayer2.Player.Previous"
bindsym $alt+c exec "dbus-send --print-reply --dest=org.mpris.MediaPlayer2.spotify /org/mpris/MediaPlayer2 org.mpris.MediaPlayer2.Player.Stop"


# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
colors {
#    separator #586e75
#    background #002b36
    background #00000000
#    statusline #aea79f
#    focused_workspace #586e75 #586e75 #ffffff
#    active_workspace #073642 #073642 #ffffff
#    inactive_workspace #002b36 #002b36 #aea79f
#    urgent_workspace #77216f #77216f #ffffff
}

        # status_command i3status
	tray_output primary	
	#strip_workspacei_numbers yes
        # status_command exec ~/programs/barista
	# font pango:noto sans mono 9
        # status_command i3blocks -c $HOME/.config/i3/i3blocks.conf
        # font pango: Awesome 9, Icons regular 9, Inconsolata 9
        # font pango: Inconsolata 9, Font Awesome 5 Free 8
	font pango: FontAwesome 5 Free 8
        # font pango: Awesome 9, Icons regular 9, Hack 9
        # font pango: FontAwesome 9, Inconsolata 9
        # font pango: Inconsolata 9
	i3bar_command i3bar --transparency
	# status_command ~/.config/i3/conky-i3bar
	# status_command exec conky -c ~/.config/i3/conkyrc
	# status_command exec --no-startup-id xfce4-panel --disable-wm-check 
	#  status_command ~/.config/bumblebee-status/bumblebee-status -m cpu memory battery time pasink pasource -p time.format="%H:%M" -t solarized
	# status_command ~/.config/bumblebee-status/bumblebee-status -m disk load spotify memory caffeine brightness battery pasink datetime shortcut indicator -p datetime.format="%a %d.%m.%y  %H:%M" -p datetime.left-click="orage" -p battery.left-click="xfce4-power-manager -c" -p load.left-click='x-terminal-emulator -T "TerminalHtop" -e htop' -p shortcut.cmds="xfce4-appfinder;xfce4-settings-manager;$HOME/bin/logout.py &>/dev/null" shortcut.labels=";;" -a indicator battery  -p pasink.label="volume" -p disk.left-click="thunar" -t gruvbox-powerline
	#last working status
	#status_command ~/.config/bumblebee-status/bumblebee-status -m disk:root disk:home load spotify memory weather brightness battery pasink shortcut indicator datetime -p disk.left_click="thunar" root.path=/ home.path=/home load.left-click='x-terminal-emulator -T "TerminalHtop" -e htop' spotify.format="{playbackStatus} {artist} - {title}" memory.label="" weather.right-click="xfce4-terminal -H -T 'Weather' -e 'curl wttr.in'" battery.left-click="xfce4-power-manager -c" shortcut.cmds="xfce4-appfinder;xfce4-settings-manager;x-terminal-emulator -T "gui-settings" -e ./own_programs/gui-settings;x-terminal-emulator -e i3lock -d -c 000000 -n;$HOME/bin/logout.py &>/dev/null" shortcut.labels=";;;;" datetime.format="%a %d.%m.%y  %H:%M" datetime.left-click="orage" indicator.include='capslock,scrollock' interval=1 engine.workspacewheel=false -a indicator -t iceberg
	status_command bumblebee-status -m title nic spotify memory weather caffeine brightness battery pasink shortcut indicator datetime -p title.max=32 nic.states="^down" spotify.format="{playbackStatus} {artist} - {title}" spotify.left-click="i3-msg '[class='Spotify'] scratchpad show'" spotify.pause=RIGHT_CLICK memory.label="" memory.left-click='x-terminal-emulator -T "TerminalHtop" -e htop' memory.format="{used}/{total}" weather.right-click="xfce4-terminal -H -T 'Weather' -e 'curl wttr.in'" battery.left-click="xfce4-power-manager -c" shortcut.cmds="xfce4-appfinder;xfce4-settings-manager;x-terminal-emulator -T "gui-settings" -e ./own_programs/gui-settings;x-terminal-emulator -e i3lock -d -c 000000 -n;$HOME/bin/logout.py &>/dev/null" shortcut.labels=";;;;" datetime.format="%a %d.%m.%y  %H:%M" datetime.left-click="orage" indicator.include='capslock,scrollock' interval=1 engine.workspacewheel=false -a indicator -t iceberg
}

#bindsym --release Caps_Lock exec pkill -SIGRTMIN+11 i3blocks
#bindsym --release Num_Lock  exec pkill -SIGRTMIN+11 i3blocks
